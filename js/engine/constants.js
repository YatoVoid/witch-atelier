// Ring radius as a fraction of the canvas size. Shared by render.js (drawing)
// and app.js (converting a drawn stroke's length into a normalized value) so
// the two never drift apart.
const RING_RATIO = 0.32;
